/*
 *
 * HomePage
 *
 */
/* eslint-disable */
import React, { memo } from "react";

const HomePage = ({ history: { push } }) => {
  return <h1>Welcome to Fault Code CMS</h1>;
};

export default memo(HomePage);
