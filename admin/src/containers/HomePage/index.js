/*
 *
 * HomePage
 *
 */
/* eslint-disable */
import React, { memo } from "react";

const HomePage = ({ history: { push } }) => {
  return <h1>Welcome to Fault Code CMSaaa</h1>;
};

export default memo(HomePage);
