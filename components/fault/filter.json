{
  "collectionName": "components_fault_filters",
  "info": {
    "name": "Filter",
    "icon": "address-card",
    "description": ""
  },
  "options": {},
  "attributes": {
    "minVersion": {
      "type": "string",
      "regex": "^([0-9]+)\\.([0-9]+)\\.([0-9]+)$"
    },
    "maxVersion": {
      "type": "string",
      "regex": "^([0-9]+)\\.([0-9]+)\\.([0-9]+)$"
    },
    "generations": {
      "collection": "generation"
    },
    "products": {
      "collection": "product"
    },
    "serviceLevels": {
      "collection": "service-level"
    }
  }
}
